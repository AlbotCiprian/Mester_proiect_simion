# Working Decisions

Permanent architecture decisions belong in `docs/specs/05-ARCHITECTURE-DECISION-RECORDS.md`.
This file records implementation-level decisions and pending proposals.

| ID | Date | Status | Decision | Reason | Affected modules | Approved by |
|---|---|---|---|---|---|---|
| D-001 | 2026-06-23 | Accepted | Single canonical `CLAUDE.md` (root, RO); removed duplicate `docs/specs/CLAUDE.md` and orphan `CLAUDE copy.md` | Only the root file is auto-loaded; avoid divergence | orchestration/config | owner |
| D-002 | 2026-06-23 | Accepted | Research consolidated under `docs/research/` (single copy); brief renamed to `MASTER_PROMPT_WEBSITE_PREMIUM.md`; `docs/specs/references/` removed | De-duplicate research; align with requested structure | docs | owner |
| D-003 | 2026-06-23 | Accepted | Added empty code scaffolding (`app/`, `components/`, `lib/`, `prisma/`, `public/`, `tests/`) + local config files; no product code before Gate A | Match requested repo tree while preserving Gate A | repo structure | owner |
| D-004 | 2026-06-24 | Accepted | Frontend design prototype started ahead of Gate A at owner's explicit request: Next.js 15 (App Router, TS strict) + Tailwind v4 tokens, custom "Precision Atelier" homepage. All copy is placeholder (CONFIRM_OWNER); imagery is open-source (Unsplash) stand-in; build is `noindex`. Production publication remains blocked by Gate A. | Owner wants an early visual; design direction (spec 06–09) can proceed on placeholders without inventing business claims | foundation, design-system, public-web | owner |
| D-005 | 2026-06-24 | Accepted | Signature scroll-cinematic module ("apartament placat", room-by-room) specified in `29-SCROLL-CINEMATIC-EXPERIENCE.md`. Mechanism = image-sequence on canvas (NOT video scrubbing); per-room price/CTA overlays; static stacked fallback for reduced-motion/no-JS. Open-source footage in prototype. | Owner request for scroll-driven video experience; image-sequence is the reliable, frame-perfect, accessible approach (extends spec 08 Variant B) | public-web, motion, estimator | owner |
| D-006 | 2026-08-18 | Accepted | All placeholder media replaced with owner-supplied real project photography. Deleted: the AI-generated hero video + posters (`hero-finisaje-premium-*`), the 122 Pexels-derived journey frames + `loop.mp4` (`public/sequences/**`, ~12 MB), the 10 `public/images/portfolio/poza*.jpg` and the dead Unsplash `hero` export in `lib/content.ts`. Published instead: 38 owner photos under `public/images/proiecte/<proiect>/` across 9 project slugs, plus a cleaned 7.5 s clip at `public/media/tur/`. Consequence of the source resolution ceiling (max 1280 px, mostly 576x1280): the **full-window hero ships as a still, `video: null`** and the **60-frame scrub tier is retired** (`JourneyMedia.scrub = null`) rather than upscaling 1280 px sources into slots designed for 1920 px. `heroMedia.rightsStatus` moved `ORIGINAL_GENERATED` -> `OWNED`. | Owner instruction to replace every asset with real work. No landscape or high-resolution footage exists, and the only clip is 480x848; shipping a sharp still beats shipping a 4x-upscaled video, and keeping Pexels/AI stand-ins was no longer acceptable. Both tiers are re-enablable by data alone once a proper shoot exists. | public-web, motion, media | owner |
| D-007 | 2026-08-18 | Accepted | Reverted the six drafted testimonials in `lib/content.ts` back to two explicit `CONFIRM_OWNER` placeholders. | `CLAUDE.md` forbids inventing recenzii; quotes labelled "(exemplu)" still read as testimonials and would have to be deleted before launch anyway. | content | owner |
| D-008 | 2026-08-19 | Accepted (amended) | Keep the 10 previously-supplied photos (`docs/poze/poza*.jpg`) alongside the 28 new ones, restricted to the categories the new set does not cover (exterior, interior locuibil, garaj/spații tehnice). | The new set is 100% băi; dropping the old set would leave `Placări exterioare`, `Interior` and `Comercial` with no truthful image, and the old files are 900x1200 vs 576x1280 — higher effective quality in a 3:4 slot. Reversible: delete the four `source: "old"` projects in `scripts/media-catalog.mjs` and re-run `npm run media`. | media, content | owner — amended 2026-08-19: only `poza1`/`poza2` (fațadă + scară) survive, exclusively for the "Placări exterioare" card and the Exterior category. The other eight old photos were withdrawn; the site is otherwise 100% `docs/poze/poze reale`. 30 published assets, 6 project slugs. |
| D-009 | 2026-08-19 | Accepted | **No database.** Leads are captured by a Server Action (`app/actions/lead.ts`) and delivered by e-mail through Resend (`lib/notify.ts`). No Prisma, no Neon, no CRM, no lead history. Consequences encoded in code: the success state may never claim the request was *saved* or offer tracking; a delivery failure returns `undelivered` and hands the visitor the phone number rather than a fake confirmation; the outbound e-mail carries the consent version and a server-side UTC timestamp because it is the only record that consent was given. | Owner decision (2026-08-19): e-mail only. A datastore he cannot operate is a dead feature (spec 01), and Resend + his phone is his real CRM. Supersedes spec 12's "salvarea în DB este tranzacția principală" and makes spec 24 Faza 4 (Admin + CRM) unsatisfiable as written — both are to be re-specified, not silently ignored. | public-web, lead-gen, data | owner |
| D-010 | 2026-08-19 | Accepted | **Doorway pages refused.** The owner asked for keyword URLs that redirect straight to the homepage, purely to be indexed. We will not build them. Instead: an evidence-gated route map where a page that cannot fill its own unique content blocks is not generated at all. Shipped now: `/ro/confidentialitate`. Deferred until owner data lands: service pages, project case studies, locality pages. | `.claude/rules/seo-accessibility.md:14` forbids doorway pages verbatim; Google follows the 3xx and drops the intermediate URL, so the tactic is worth zero incremental traffic even when it works, and a site-scoped spam classification would damage `xelacktech.com` as well. The legitimate route to the same revenue is fewer, better pages plus a Google Business Profile. | seo, public-web | owner |
| D-011 | 2026-08-19 | Accepted | **One indexability predicate**, `INDEXABLE` in `lib/seo.ts` = production deployment AND confirmed host AND `GATE_A_COMPLETE`. Consumed by exactly four places: root layout `robots`, `app/robots.ts`, `app/sitemap.ts`, `app/llms.txt/route.ts`. Unverifiable content is now **data-gated, not comment-gated**: `trustFacts`, `reviews` and `faqs` are empty arrays and their sections do not render. | A `CONFIRM_OWNER` comment does not stop a deploy; an empty array does. Previously "10+ ani", "200+ proiecte", "24 luni garanție" and five placeholder FAQ answers rendered as real public claims, which `CLAUDE.md` forbids and which is a consumer-protection exposure. | seo, content | owner |
| D-012 | 2026-08-19 | Accepted | **The bot time-trap observes, it does not block.** A submission faster than 1.5s is logged as `fast=true` and delivered anyway. Only the honeypot discards silently. | Browser autofill legitimately fills name, phone and e-mail instantly, so a real high-intent visitor can submit in under a second — this was caught in testing, where a genuine submission was discarded with a fake success. With no datastore, a false positive loses a customer permanently and invisibly, while a false negative costs one spam e-mail. The asymmetry is not close. | lead-gen, security | owner |
| D-013 | 2026-08-19 | Accepted | **One indexability predicate, shared with the build config.** `config/indexability.mjs` exports `GATE_A_COMPLETE`, `normalizeHost()` and `indexability()`. `lib/seo.ts` and `next.config.mjs` both consume it; `lib/content.ts` re-exports the gate. Host comparison is normalised (scheme, trailing slash, case, credentials, FQDN dot) and the build THROWS when a production deployment has Gate A open but would still be noindex. | There were two predicates. `X-Robots-Tag` was emitted only when `VERCEL_ENV !== "production"`, so production-before-Gate-A was protected by `Disallow: /` alone — and a disallowed URL is one whose noindex Google can never read, the exact failure `app/robots.ts` warns about. Separately, a scheme or trailing slash difference between the two host variables produced a green build and an invisible site with no error anywhere; that is now a loud build failure and is covered by a 20-case test suite. | seo, config | owner |
| D-014 | 2026-08-19 | Accepted | **Idempotency records only DELIVERED leads.** `app/actions/lead.ts` stores the fingerprint after a successful send and replays the original `LeadResult`; a failed delivery is deliberately not recorded, so pressing Submit again is a real retry. | The previous order registered the fingerprint before attempting delivery and never rolled it back. With Resend unprovisioned that meant EVERY second submission rendered "Cererea a plecat spre noi" with a reference number for a lead nobody received — permanent, silent customer loss, and precisely what the `undelivered` state exists to prevent. Reproduced in a browser, fixed, and covered by a Playwright retry check. The residual risk is inverted and far cheaper: two truly concurrent submissions can both send one duplicate email. | lead-gen | owner |
| D-015 | 2026-08-19 | Accepted | **Whitespace is whitespace.** The invisible-character class in `lib/lead.ts` no longer covers TAB, LF and CR; single-line fields fold them to a space and the message field preserves paragraph breaks. | `clean()` replaced the whole C0 range with the empty string, so every multi-line message had its line breaks DELETED and the words on either side fused: "Baie 6 m2.
Gresie 60x60." became "Baie 6 m2.Gresie 60x60.". It corrupted the only free-text field on the site. Covered by tests/free-text.test.ts. | lead-gen | owner |
| D-016 | 2026-08-19 | Accepted | **Data-gating extended to every remaining placeholder.** The five visitor-facing `CONFIRM_OWNER` strings and the em-dash flagship metrics no longer render. Verified: zero occurrences of `CONFIRM_OWNER`, `provizori` or `Previzualizare de design` in the rendered HTML. | D-011 gated `trustFacts`, `reviews` and `faqs` and stopped there, leaving internal review markers in the footer, the portfolio intro, the journey price card and the journey caption. An unconfirmed value must be ABSENT, not annotated for the visitor. | content, seo | owner |
| D-017 | 2026-08-19 | Accepted | **zod stays server-side.** Plain constants and result types moved to `lib/lead-labels.ts`, which imports no zod; `lib/lead.ts` re-exports them for the server. The client form imports only the label module. | Importing six constants from the schema module pulled zod into the client bundle. Measured: First Load JS 139 kB -> 120 kB, -19 kB brotli, with validation unchanged — the Server Action still parses with the full schema. | performance, lead-gen | owner |
| D-018 | 2026-09-03 | Accepted | **The company is SemiDom.** `lib/content.ts` `site.name`/`site.shortName` are the single source; the header superscript became "Ceramică"; the hero h1 became "Montaj gresie și faianță în Chișinău, executat cu precizie". Production will live on a subdomain of `xelacktech.com` (label still to be chosen). | Owner decision. It closes checklist A1, one of the seven conditions gating `GATE_A_COMPLETE` — indexing under a name that later changes forfeits the first months of ranking. "Atelier Teracota" was never approved, and it named the one service with zero supporting photographs; "Atelier" also reads as «ателье» (a dressmaker) to the Russian-speaking half of Chișinău. The h1 previously led with that same unevidenced service and carried no locality. | brand, content, seo | owner |
| D-019 | 2026-09-03 | Accepted | **Two gates, not one.** `CONTENT_COMPLETE` (is the business content final?) is now separate from `GATE_A_COMPLETE` (may Google index this?). The "site în lucru" banner is gated on the former. | The banner was gated on indexability, so it stayed visible for the entire soft-launch — exactly the window in which the owner shares the link by hand to win his first customers. Telling those visitors the content is provisional is the worst possible moment to say it. Its copy had also become false: the name is now confirmed. | content, ux | owner |
| D-020 | 2026-09-03 | Accepted | **Lead attribution is captured at submit time**, reduced to a closed allowlist in `lib/lead-source.ts` and printed as a `Sursă` row in the lead email. The phone is printed in both E.164 and national format. | The email is the only record a lead ever gets, so anything not written into it is lost forever — a lead that arrives without a source can never be re-attributed. Every email previously printed the same hardcoded path, making every lead indistinguishable. The allowlist means a crafted query string cannot forge a row. Both phone formats because the owner searches his mailbox for whatever the caller reads out, which is the national form. | lead-gen, analytics | owner |
| D-021 | 2026-09-03 | Accepted | **The form offers only CONFIRMED contact channels.** `PREFERENCE_CHANNEL` maps each preference to the channel type that must be confirmed; with one confirmed channel the select becomes a hidden input. The honeypot was renamed off `website` and is now `display:none`. | The select offered WhatsApp, Viber and Telegram while `publicChannels` hid them everywhere else — someone choosing Viber would have waited for a message that never comes. `website` is one of the most commonly autofilled field names in existence, and a password manager filling a merely off-screen honeypot would have silently discarded a real lead. Both are now bound by tests; the first mapping attempt compared "telefon" to "phone", produced an empty list and rejected every valid lead — caught in browser testing, which is why the test exists. | lead-gen | owner |
